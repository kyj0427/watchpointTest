import CommunityClient from "@/components/sections/community/CommunityClient"; // Import the client-side wrapper

const Page = () => {
  return (
    <main>
      <CommunityClient /> {/* Use the client-side component */}
    </main>
  );
};

export default Page;
